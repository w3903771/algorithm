# 参与贡献

欢迎纠错与补充。**最有价值的两类**：正文里的事实错误，以及题解跑不过的用例。

不想动手也没关系——直接开 [Issue](https://github.com/w3903771/algorithm/issues/new/choose)
写清哪一章哪一行，同样有用。

---

## 一、动手之前

```bash
git clone https://github.com/w3903771/algorithm.git
cd algorithm
uv sync
uv run mkdocs serve      # http://127.0.0.1:8000
```

需要 Python 3.9+ 与 [uv](https://docs.astral.sh/uv/)。
**CI 跑的也是 3.9**——全书代码承诺兼容 3.9，验它的那道闸门就得站在 3.9 上。

## 二、改什么，在哪改

| 想改 | 改哪个文件 | 改完跑什么 |
| --- | --- | --- |
| 一段正文 | `docs/<主题>/<slug>.md` | `uv run mkdocs serve` 看渲染 |
| 一份题解 | `solutions/<site>/<题号>/sol.py` | 见下面的「⚠️ 关于 `verify.py`」 |
| 题解页上的讲解文字 | 同上——`sol.py` 的**文档字符串就是题解页正文** | `uv run mkdocs serve` 看渲染 |
| 某章挂哪几道例题 | `data/_mapping.json` | `uv run python scripts/check_orphan.py` |

### ⚠️ 关于 `verify.py`

**它在 clone 下来的检出里跑不了。** 它要读各题的官方样例，
而题面与样例不入库（体量与版权，`data/_problems.json` 只是题目元信息的快照）。
跑它会得到一串 `NO_PROBLEM`，脚本会在末尾提示你这件事，
并且**不会**把「没验成」写回仓库里已有的验证结论。

**改了题解想自测**：自己造几组输入手动跑一遍 `sol.py`，
在 PR 里写清**用例与预期输出**，维护者会替你过一遍判题机。

**这些别手改**，它们是生成的，重跑即覆盖：

- `docs/appendix/a-problems.md`（附录 A 题单总索引）
- `README.md` 的「进度」两张表
- 各目录索引页里的章数与题数
- 站点上的一切统计数字——正文里只有 `<!-- N:… -->` 占位符，构建期注入

## 三、写作口径

正文有一套统一的文风约定，`scripts/check_prose.py` 是它的可执行版，**CI 会自动跑**。
提 PR 之前先自己跑一次：

```bash
uv run python scripts/check_prose.py
```

它用的是**棘轮基线**：每个「文件 × 条款」记着当前处数，只在**超出**时失败。
换句话说——历史遗留的不拦你，但**你新写的不许违反**。

最常踩到的几条：

| 条款 | 不许写 | 为什么 |
| --- | --- | --- |
| **P6 / P7** | 叙述里出现平台名（牛客 / 力扣 / 洛谷），链接标签带平台名 | 平台归属只保留在附录 A、题解页元信息、README 题源表三处 |
| **P10 / P11 / P12** | 第二人称「你」、第一人称「我们」、「本书 / 本教程」自指 | 正文的立场是「读者手上只有这本书」 |
| **P13** | 叙述写作或调试过程 | 读者要的是结论，不是过程 |
| **P14** | 「值得注意的是」「总的来说」这类空转连接词 | 一句话能说完就别铺垫 |
| **P15** | 「很快」「效率高」这类无量纲结论 | 要写就写清规模、环境、耗时 |
| **S1 / S2** | `##` 标题带全书章号 | 章号由 nav 决定，写进标题会把锚点钉死 |
| **R5** | 章末缺「本章速查」 | 每章要能单独当速查页用 |

真觉得某一条在你的场景下不成立，**在 PR 里说明理由**——
规范本身也可以改，但要改在正本上，不是绕过去。

## 四、提交 PR

1. Fork → clone → **切一个新分支**（别直接在 `main` 上改）；
2. 改动，本地跑一遍 `mkdocs serve` 确认渲染正常；
3. 改了正文就跑 `check_prose.py`；动过任何 `.py` 都跑一次
   `uv run python scripts/check_syntax.py`——**全书代码承诺兼容 Python 3.9**，
   `match`（3.10）、`int | None` 类型联合（3.10）这类新语法都会被它拦下。
   改了题解**不必**跑 `verify.py`（跑不了，见上），把用例写在 PR 里即可；
4. commit 信息写清**改了什么、为什么**，一句话即可；
5. 推到你的 fork，提 PR。

PR 里请说明：

- 改的是哪一章 / 哪一道题
- 如果是纠错：**原文错在哪、正确的是什么、依据是什么**
- 如果是题解：跑过哪些用例

## 五、报 Issue

两类模板，按提示填就行：

- **内容纠错**——哪一章、哪一行、原文是什么、应该是什么
- **题解问题**——题号、哪个用例、期望输出与实际输出、报错信息

写清「在哪、是什么、应该是什么」这三件事，比长篇描述有用得多。

## 六、许可

提交贡献即表示同意：正文部分按 **CC BY-NC-SA 4.0**、代码部分按 **MIT** 授权发布
（见 [LICENSE](LICENSE) 与 README 的「许可证」一节）。
