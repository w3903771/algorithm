"""PIO18 单组_spj判断数组之和 —— 构造 n 个正整数，和为 m。

要点：spj 题只要求「满足条件」而非「和标程一致」。
      最简单的构造：前 n-1 个填 1，最后一个填 m-(n-1)。
      因为保证 n <= m，所以最后一个数必然 >= 1。
"""
n, m = map(int, input().split())
print(" ".join(["1"] * (n - 1) + [str(m - (n - 1))]))
